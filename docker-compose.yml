services:
  quizlab:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 9080:9080
    env_file:
      - .env
    volumes:
      - ./data:/home/uv-app-user/app/data
    networks:
      - project_network

networks:
  project_network:
    external: true